install node.js
install yarn(opsional)(prompt)
install git


1. Install project next JS(prompt)
	- npx create-next-app@latest
	- memberi nama project
	- Typescript yes
	- tailwind ? yes
	- ESlint? yes
	- src/ directory no
	- app router? no 

2. install nextauth
	-npm install next-auth(prompt)
